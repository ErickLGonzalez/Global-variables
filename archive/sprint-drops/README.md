# Sprint drop archives

Incoming sprint bundles extracted into the repo, then archived here for provenance.

| Archive | Integrated commit | Contents |
|---|---|---|
| `gv-s2b-exact-kernel.zip` | `de69e78` | S2-b exact-kernel GNS probe (`s2_gns/s2b_probe.py`, `tests/test_s2b.py`) |
| `gv-s3-alphas.zip` | (this sprint) | S3 PDG α_s layer (`s3_pdg/`, `tests/test_s3.py`, edit-008 memo) |

Extracted trees (`*-extract/`) are ephemeral and gitignored; use these zips if you need the original drop layout.
